# NASA FIRMS — the one upstream that needs a server-side key.
# Used only by functions/api/fire.ts, never exposed to the browser.
# Set for local dev in .dev.vars, and in the Cloudflare Pages dashboard for production.
FIRMS_API_KEY=
